CREATE TABLE IF NOT EXISTS {{ .Name }} (
      id BIGINT UNSIGNED,
      alt_label VARCHAR(255) NOT NULL,
      body JSON NOT NULL,
      lastmodified INT NOT NULL,
      UNIQUE KEY {{ .Name }}_id_alt (id, alt_label),
      KEY {{ .Name }}_lastmodified (lastmodified)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;